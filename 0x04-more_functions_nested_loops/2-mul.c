#include "main.h"
#include <stdio.h>

/**
 * mul - This function multiply two integers
 * @a:the furst number
 * @b:the seconde number to be multiply
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
